#!/usr/bin/env bash
# Personal data in a commit message, which no tree scan sees. Runs after the
# full gate, so its rule has no false positives.
set -euo pipefail
exec nix run "git+file:../dev-lint?ref=HEAD#dev-lint" -- --commit-msg "$1"
